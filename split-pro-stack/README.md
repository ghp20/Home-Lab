# Split Pro Stack

> Split Pro is an expense splitting application for sharing costs among groups, roommates, or travel companions.

**Status:** 🔴 Inactive  
**Stack:** `split-pro-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `ossapps/postgres:${PG_TAG:-17.7-trixie}` | — | 🔴 — |
| `splitpro` | `ossapps/splitpro:${SPLITPRO_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `${STORAGE_ROOT}/postgres17:/var/lib/postgresql/data` | Config/data |
| `splitpro` | `${HOST_PORT}:3000` | Config/data |
| `splitpro` | `${STORAGE_ROOT}/uploads:/app/uploads` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `split-pro-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:24 UTC_

# Portracker Stack

> PortRacker is a network port tracking and service inventory tool for monitoring open ports across your infrastructure.

**Status:** 🟢 Active  
**Stack:** `portracker-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `portracker` | `mostafawahied/portracker:${PT_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `portracker` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `portracker` | `apparmor:unconfined` | Config/data |
| `portracker` | `${STORAGE_ROOT}:/data` | Config/data |
| `portracker` | `${DOCKER_SOCK}:/var/run/docker.sock:ro` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `portracker` | `DATABASE_PATH` | `${DATABASE_PATH}` |
| `portracker` | `PORT` | `${CONTAINER_PORT}` |
| `portracker` | `TRUENAS_API_KEY` | `changeme` |
| `portracker` | `TRUENAS_WS_BASE` | `${TRUENAS_WS_BASE}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `portracker-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-24 17:00 UTC_

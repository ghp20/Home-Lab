# Tdarr Stack

> Tdarr is a distributed transcoding system for automating media library health checks and transcoding with FFmpeg and HandBrake.

**Status:** 🔴 Inactive  
**Stack:** `tdarr-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `tdarr` | `ghcr.io/haveagitgat/tdarr:${TDARR_TAG:-latest}` | — | 🔴 — |
| `tdarr-node` | `ghcr.io/haveagitgat/tdarr_node:${NODE_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `tdarr` | `${UI_PORT}:${UI_PORT}` | Config/data |
| `tdarr` | `${API_PORT}:${API_PORT}` | Config/data |
| `tdarr` | `${STORAGE_ROOT}/server:/app/server` | Config/data |
| `tdarr` | `${STORAGE_ROOT}/config:/app/configs` | Config/data |
| `tdarr` | `${STORAGE_ROOT}/log:/app/logs` | Config/data |
| `tdarr` | `${TEMP_ROOT}:/temp` | Config/data |
| `tdarr` | `${MEDIA_ROOT}/:/media` | Config/data |
| `tdarr` | `${MEDIA_ROOT}/Movies:/output/movies` | Config/data |
| `tdarr` | `${MEDIA_ROOT}/Tv:/output/tv` | Config/data |
| `tdarr` | `/dev/dri/renderD128:/dev/dri/renderD128` | Config/data |
| `tdarr` | `/dev/dri/card0:/dev/dri/card0` | Config/data |
| `tdarr-node` | `${STORAGE_ROOT}/config:/app/configs` | Config/data |
| `tdarr-node` | `${STORAGE_ROOT}/log:/app/logs` | Config/data |
| `tdarr-node` | `${TEMP_ROOT}:/temp` | Config/data |
| `tdarr-node` | `${MEDIA_ROOT}/:/media` | Config/data |
| `tdarr-node` | `${MEDIA_ROOT}/Movies:/output/movies` | Config/data |
| `tdarr-node` | `${MEDIA_ROOT}/Tv:/output/tv` | Config/data |
| `tdarr-node` | `/dev/dri/renderD128:/dev/dri/renderD128` | Config/data |
| `tdarr-node` | `/dev/dri/card0:/dev/dri/card0` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `tdarr` | `TZ` | `${TZ}` |
| `tdarr` | `PUID` | `${PUID}` |
| `tdarr` | `PGID` | `${PGID}` |
| `tdarr` | `UMASK` | `002` |
| `tdarr-node` | `TZ` | `${TZ}` |
| `tdarr-node` | `PUID` | `${PUID}` |
| `tdarr-node` | `PGID` | `${PGID}` |
| `tdarr-node` | `UMASK` | `002` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `tdarr-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Device mapping:** `tdarr` maps a device (`/dev/dri/renderD128:/dev/dri/renderD128`). Ensure the device exists on the host.
3. **Device mapping:** `tdarr-node` maps a device (`/dev/dri/renderD128:/dev/dri/renderD128`). Ensure the device exists on the host.

---
_Auto-generated by Alex • Last updated: 2026-08-03 17:00 UTC_

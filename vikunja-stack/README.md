# Vikunja Stack

> Vikunja is an open-source to-do and project management application with kanban boards, lists, and team collaboration features.

**Status:** 🟢 Active  
**Stack:** `vikunja-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `vikunja` | `vikunja/vikunja:${VIKUNJA_TAG:-latest}` | — | 🔴 — |
| `db` | `postgres:${POSTGRES_TAG:-18}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `vikunja` | `${STORAGE_ROOT}/files:/app/vikunja/files` | Config/data |
| `vikunja` | `${VIKUNJA_PORT:-3456}:3456` | Config/data |
| `db` | `${STORAGE_ROOT}/db:/var/lib/postgresql` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `vikunja-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`vikunja_net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-06-01 17:00 UTC_

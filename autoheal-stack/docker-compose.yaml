version: "3.9"

services:
  autoheal:
    image: willfarrell/autoheal:${AUTOHEAL_TAG:-latest}
    container_name: autoheal
    restart: unless-stopped
    environment:
      - AUTOHEAL_INTERVAL=changeme
      - AUTOHEAL_START_PERIOD=changeme
    volumes:
      - ${DOCKER_SOCK}:/var/run/docker.sock
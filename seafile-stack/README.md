# Seafile Stack

> The Seafile Stack stack.

**Status:** 🟢 Active  
**Stack:** `seafile-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `db` | `mariadb:10.11.6` | — | 🔴 — |
| `memcached` | `memcached:1.6.23` | — | 🔴 — |
| `elasticsearch` | `elasticsearch:8.13.4` | — | 🔴 — |
| `seafile` | `seafileltd/seafile-mc:${SEAFILE_VERSION_TAG}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `db` | `/path/to/data/apps_config/seafile/db:/var/lib/mysql` | Config/data |
| `elasticsearch` | `/path/to/data/apps_config/seafile/elasticsearch:/usr/share/elasticsearch/data` | Config/data |
| `seafile` | `${SEAFILE_HTTP_PORT}:80` | Config/data |
| `seafile` | `/path/to/data/apps_config/seafile/shared:/shared` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `db` | `MYSQL_ROOT_PASSWORD` | `changeme` |
| `db` | `MYSQL_LOG_CONSOLE` | `${MYSQL_LOG_CONSOLE}` |
| `elasticsearch` | `ES_JAVA_OPTS` | `${ES_JAVA_OPTS}` |
| `seafile` | `DB_HOST` | `${DB_HOST}` |
| `seafile` | `DB_ROOT_PASSWD` | `${DB_ROOT_PASSWD}` |
| `seafile` | `SEAFILE_ADMIN_EMAIL` | `${SEAFILE_ADMIN_EMAIL}` |
| `seafile` | `SEAFILE_ADMIN_PASSWORD` | `changeme` |
| `seafile` | `SEAFILE_SERVER_LETSENCRYPT` | `${SEAFILE_LETSENCRYPT}` |
| `seafile` | `SEAFILE_SERVER_HOSTNAME` | `${SEAFILE_SERVER_HOSTNAME}` |
| `seafile` | `TIME_ZONE` | `${TIME_ZONE}` |
| `seafile` | `FORCE_HTTPS_IN_SEAFILE_PRO` | `${FORCE_HTTPS}` |
| `seafile` | `JWT_PRIVATE_KEY` | `changeme` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `seafile-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`seafile-net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-07-27 17:01 UTC_

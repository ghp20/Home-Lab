# Onlyoffice Stack

> ONLYOFFICE Docs is an online office suite for creating, editing, and collaborating on documents, spreadsheets, and presentations in real time.

**Status:** 🟢 Active  
**Stack:** `onlyoffice-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `onlyoffice-document-server` | `onlyoffice/documentserver:${OO_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `onlyoffice-document-server` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `onlyoffice-document-server` | `${STORAGE_ROOT}/data:/var/www/onlyoffice/Data` | Config/data |
| `onlyoffice-document-server` | `${STORAGE_ROOT}/logs:/var/log/onlyoffice` | Config/data |
| `onlyoffice-document-server` | `JWT_ENABLED=${JWT_ENABLED:-true}` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `onlyoffice-document-server` | `JWT_SECRET` | `changeme` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `onlyoffice-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `always` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-03 17:00 UTC_

# Spartans Dashboard Stack

> The Spartans Dashboard Stack stack.

**Status:** 🟢 Active  
**Stack:** `spartans-dashboard-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `postgres:15` | — | 🔴 — |
| `backend` | `spartans-dashboard-backend:latest` | — | 🔴 — |
| `frontend` | `spartans-dashboard-frontend:latest` | 8102:8080 | 🔴 — |

## 🔌 Port Reference

| Service | Mapping | Protocol |
|---------|---------|----------|
| `frontend` | `8102:8080` | TCP |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `dashboard-pgdata:/var/lib/postgresql/data` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `spartans-dashboard-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`dashboard-net, spartans-events_backend-net, spartans-logistics_backend-net, spartans-maintenance_backend-net, spartans-practice_backend-net, spartans-platform-net`). Other stacks sharing these networks can communicate directly.
4. **Named volumes:** `dashboard-pgdata` are persisted as Docker volumes. Back these up regularly.

---
_Auto-generated by Alex • Last updated: 2026-07-06 17:00 UTC_

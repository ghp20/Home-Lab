# Immich Stack

> Immich is a self-hosted photo and video backup solution, an alternative to Google Photos with ML-powered face detection and search.

**Status:** 🟢 Active  
**Stack:** `immich-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `immich-server` | `ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-v1.106.1}` | — | 🔴 — |
| `immich-machine-learning` | `ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-v1.106.1}-cuda` | — | 🔴 — |
| `redis` | `valkey/valkey:${VALKEY_VERSION:-latest}` | — | 🔴 — |
| `database` | `ghcr.io/immich-app/postgres:${PG_VERSION:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `immich-server` | `${STORAGE_ROOT}/data:/data` | Config/data |
| `immich-server` | `${EXTERNAL_PATH}:/external` | Config/data |
| `immich-server` | `/etc/localtime:/etc/localtime:ro` | Config/data |
| `immich-server` | `NODE_ENV=${NODE_ENV:-production}` | Config/data |
| `immich-server` | `IMMICH_MACHINE_LEARNING_URL=http://immich-machine-learning:3003` | Config/data |
| `immich-server` | `${IMMICH_PORT}:${IMMICH_PORT}` | Config/data |
| `immich-server` | `driver: nvidia` | Config/data |
| `immich-machine-learning` | `immich-ml-cache:/mlcache` | Config/data |
| `immich-machine-learning` | `NODE_ENV=${NODE_ENV:-production}` | Config/data |
| `immich-machine-learning` | `driver: nvidia` | Config/data |
| `database` | `${STORAGE_ROOT}/postgres:/var/lib/postgresql` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `immich-server` | `IMMICH_PORT` | `${IMMICH_PORT}` |
| `immich-server` | `DB_HOSTNAME` | `${DB_HOSTNAME}` |
| `immich-server` | `DB_USERNAME` | `${DB_USERNAME}` |
| `immich-server` | `DB_DATABASE_NAME` | `${DB_DATABASE_NAME}` |
| `immich-server` | `DB_PASSWORD` | `changeme` |
| `immich-server` | `REDIS_HOSTNAME` | `${REDIS_HOSTNAME}` |
| `immich-server` | `REDIS_PASSWORD` | `changeme` |
| `immich-server` | `PUID` | `${PUID}` |
| `immich-server` | `PGID` | `${PGID}` |
| `immich-server` | `TZ` | `${TZ}` |
| `immich-machine-learning` | `PUID` | `${PUID}` |
| `immich-machine-learning` | `PGID` | `${PGID}` |
| `immich-machine-learning` | `TZ` | `${TZ}` |
| `database` | `POSTGRES_PASSWORD` | `changeme` |
| `database` | `POSTGRES_USER` | `${DB_USERNAME}` |
| `database` | `POSTGRES_DB` | `${DB_DATABASE_NAME}` |
| `database` | `PGDATA` | `/var/lib/postgresql/18/docker` |
| `database` | `PUID` | `${PUID}` |
| `database` | `PGID` | `${PGID}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `immich-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `redis, database`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `always` — containers will restart automatically unless explicitly stopped.
3. **Named volumes:** `immich-ml-cache` are persisted as Docker volumes. Back these up regularly.

---
_Auto-generated by Alex • Last updated: 2026-07-13 17:00 UTC_

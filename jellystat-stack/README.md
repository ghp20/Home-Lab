# Jellystat Stack

> JellyStat is a statistics and monitoring dashboard for Jellyfin, tracking play sessions, user activity, and library metrics.

**Status:** 🟢 Active  
**Stack:** `jellystat-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `jellystat-db` | `postgres:${PG_TAG:-17}` | — | 🔴 — |
| `jellystat` | `cyfershepard/jellystat:${JELLYSTAT_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `jellystat-db` | `${STORAGE_ROOT}/postgres:/var/lib/postgresql/data` | Config/data |
| `jellystat` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `jellystat` | `${STORAGE_ROOT}/backup:/app/backend/backup-data` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `jellystat` | `TZ` | `${TZ}` |
| `jellystat` | `POSTGRES_USER` | `${POSTGRES_USER}` |
| `jellystat` | `POSTGRES_PASSWORD` | `changeme` |
| `jellystat` | `POSTGRES_IP` | `jellystat-db` |
| `jellystat` | `POSTGRES_PORT` | `5432` |
| `jellystat` | `POSTGRES_DB` | `${POSTGRES_DB}` |
| `jellystat` | `JWT_SECRET` | `changeme` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `jellystat-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `jellystat-db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-10 17:00 UTC_

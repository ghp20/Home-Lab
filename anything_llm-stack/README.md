# Anything_Llm Stack

> AnythingLLM is an all-in-one AI document chatbot — connect LLMs to your documents for question-answering and RAG workflows.

**Status:** 🔴 Inactive  
**Stack:** `anything_llm-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `anything-llm` | `mintplexlabs/anythingllm:${ALLM_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `anything-llm` | `DISABLE_TELEMETRY=${DISABLE_TELEMETRY:-1}` | Config/data |
| `anything-llm` | `${STORAGE_ROOT}:/app/server/storage` | Config/data |
| `anything-llm` | `${STORAGE_ROOT}:/app/storage` | Config/data |
| `anything-llm` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `anything-llm` | `SERVER_PORT` | `${CONTAINER_PORT}` |
| `anything-llm` | `STORAGE_DIR` | `${STORAGE_DIR}` |
| `anything-llm` | `DATABASE_URL` | `${DATABASE_URL}` |
| `anything-llm` | `JWT_SECRET` | `changeme` |
| `anything-llm` | `ENCRYPTION_KEY` | `changeme` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `anything_llm-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-10 17:00 UTC_

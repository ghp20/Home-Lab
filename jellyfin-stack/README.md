# Jellyfin Stack

> Jellyfin is a free and open-source media system that lets you stream your media to any device from your own server, with no subscription fees.

**Status:** 🟢 Active  
**Stack:** `jellyfin-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `jellyfin` | `lscr.io/linuxserver/jellyfin:${JELLYFIN_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `jellyfin` | `driver: nvidia` | Config/data |
| `jellyfin` | `${APPS_CONFIG}/config:/config` | Config/data |
| `jellyfin` | `${APPS_CONFIG}/cache:/cache` | Config/data |
| `jellyfin` | `${APPS_CONFIG}/transcode:/transcode` | Config/data |
| `jellyfin` | `${MEDIA_ROOT}:/media:ro` | Config/data |
| `jellyfin` | `${HTTP_PORT}:8096` | Config/data |
| `jellyfin` | `${HTTPS_PORT}:8920` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `jellyfin` | `TZ` | `${TZ}` |
| `jellyfin` | `PUID` | `${PUID}` |
| `jellyfin` | `PGID` | `${PGID}` |
| `jellyfin` | `NVIDIA_VISIBLE_DEVICES` | `all` |
| `jellyfin` | `NVIDIA_DRIVER_CAPABILITIES` | `compute,video,utility` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `jellyfin-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-06-01 17:00 UTC_

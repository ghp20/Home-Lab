# Plex Stack

> Plex Media Server organizes video, music, and photos from personal media libraries and streams them to smart TVs, streaming boxes, and mobile devices.

**Status:** 🔴 Inactive  
**Stack:** `plex-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `plex` | `lscr.io/linuxserver/plex:${PLEX_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `plex` | `${STORAGE_ROOT}/config:/config` | Config/data |
| `plex` | `${STORAGE_ROOT}/transcode:/transcode` | Config/data |
| `plex` | `${MEDIA_ROOT}:/media` | Config/data |
| `plex` | `${PLEX_PORT}:${PLEX_PORT}` | Config/data |
| `plex` | `/dev/dri:/dev/dri` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `plex` | `PUID` | `${PUID}` |
| `plex` | `PGID` | `${PGID}` |
| `plex` | `TZ` | `${TZ}` |
| `plex` | `VERSION` | `${VERSION}` |
| `plex` | `PLEX_CLAIM` | `${PLEX_CLAIM}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `plex-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Device mapping:** `plex` maps a device (`/dev/dri:/dev/dri`). Ensure the device exists on the host.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:23 UTC_

# Paperlesngx Stack

> Paperless-ngx is a document management system that scans, indexes, and archives physical documents with OCR and full-text search.

**Status:** 🟢 Active  
**Stack:** `paperlesngx-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `postgres:${PG_TAG:-17}` | — | 🔴 — |
| `redis` | `redis:${REDIS_TAG:-7}` | — | 🔴 — |
| `tika` | `apache/tika:${TIKA_TAG:-latest}` | — | 🔴 — |
| `gotenberg` | `gotenberg/gotenberg:${GOTENBERG_TAG:-8}` | — | 🔴 — |
| `paperless` | `ghcr.io/paperless-ngx/paperless-ngx:${PAPERLESS_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `${STORAGE_ROOT}/postgres/17/docker:/var/lib/postgresql/data` | Config/data |
| `paperless` | `${PAPERLESS_PORT:-30070}:8000` | Config/data |
| `paperless` | `${STORAGE_ROOT}/data:/usr/src/paperless/data` | Config/data |
| `paperless` | `${STORAGE_ROOT}/media:/usr/src/paperless/media` | Config/data |
| `paperless` | `${STORAGE_ROOT}/consume:/usr/src/paperless/consume` | Config/data |
| `paperless` | `${STORAGE_ROOT}/trash:/usr/src/paperless/trash` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `paperlesngx-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres, redis`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-31 17:01 UTC_

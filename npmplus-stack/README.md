# Npmplus Stack

> NPM Plus — an enhanced version of Nginx Proxy Manager with additional features for advanced reverse proxy management.

**Status:** 🔴 Inactive  
**Stack:** `npmplus-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `npmplus` | `zoeyvid/npmplus:${NPMPLUS_TAG:-2026-04-12-r1}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `npmplus` | `${HTTPS_PORT}:443/tcp` | Config/data |
| `npmplus` | `${HTTPS_PORT}:443/udp` | Config/data |
| `npmplus` | `${ADMIN_PORT}:81/tcp` | Config/data |
| `npmplus` | `${STORAGE_ROOT}:/data` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `npmplus` | `PUID` | `${PUID}` |
| `npmplus` | `PGID` | `${PGID}` |
| `npmplus` | `TZ` | `${TZ}` |
| `npmplus` | `DISABLE_HTTP` | `true` |
| `npmplus` | `CROWDSEC_LAPI_URL` | `${CROWDSEC_LAPI_URL}` |
| `npmplus` | `CROWDSEC_LAPI_KEY` | `changeme` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `npmplus-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `always` — containers will restart automatically unless explicitly stopped.
2. **Custom networks:** This stack defines custom networks (`default, crowdsec_net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-05-25 17:01 UTC_

# Spartans Logistics

> Spartans Logistics extends the Spartans platform with supply chain and logistics tracking features.

**Status:** 🟢 Active  
**Stack:** `spartans-logistics`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `db` | `postgres:${PG_TAG:-16-alpine}` | — | 🔴 — |
| `backend` | `spartans-backend:${BACKEND_TAG:-latest}` | 8003:8000 | 🔴 — |
| `frontend` | `spartans-frontend:${FRONTEND_TAG:-latest}` | — | 🔴 — |

## 🔌 Port Reference

| Service | Mapping | Protocol |
|---------|---------|----------|
| `backend` | `8003:8000` | TCP |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `db` | `${STORAGE_ROOT}/db:/var/lib/postgresql/data` | Config/data |
| `backend` | `${STORAGE_ROOT}/backend:/app/assets` | Config/data |
| `frontend` | `${HOST_PORT}:80` | Config/data |
| `frontend` | `${STORAGE_ROOT}/frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro` | Config/data |
| `frontend` | `${STORAGE_ROOT}/frontend/logo.png:/usr/share/nginx/html/logo.png:ro` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `spartans-logistics`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`backend-net, spartans-platform-net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-07-20 17:00 UTC_

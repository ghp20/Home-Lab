# Home Assistant Stack

> Home Assistant is an open-source home automation platform that integrates with thousands of smart devices and services for local control.

**Status:** 🟢 Active  
**Stack:** `home-assistant-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `postgres:${PG_TAG:-17}` | — | 🔴 — |
| `homeassistant` | `ghcr.io/home-assistant/home-assistant:${HA_TAG:-stable}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `${STORAGE_ROOT}/postgres:/var/lib/postgresql/data` | Config/data |
| `homeassistant` | `${STORAGE_ROOT}/config:/config` | Config/data |
| `homeassistant` | `${STORAGE_ROOT}/media:/media` | Config/data |
| `homeassistant` | `/etc/localtime:/etc/localtime:ro` | Config/data |
| `homeassistant` | `/run/dbus:/run/dbus:ro` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `home-assistant-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-06-29 17:00 UTC_

# Changedetection Stack

> Changedetection.io monitors websites for changes and sends notifications when content is added, removed, or modified.

**Status:** 🟢 Active  
**Stack:** `changedetection-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `changedetection` | `ghcr.io/dgtlmoon/changedetection.io:latest` | 30159:5000 | 🔴 — |
| `browser-sockpuppet-chrome` | `dgtlmoon/sockpuppetbrowser:latest` | — | 🔴 — |

## 🔌 Port Reference

| Service | Mapping | Protocol |
|---------|---------|----------|
| `changedetection` | `30159:5000` | TCP |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `changedetection` | `/path/to/data/apps_config/changedetection/datastore:/datastore` | Config/data |
| `changedetection` | `no-new-privileges:true` | Config/data |
| `changedetection` | `/tmp:size=256m,mode=1777` | Config/data |
| `browser-sockpuppet-chrome` | `no-new-privileges:true` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `changedetection` | `TZ` | `${TZ}` |
| `changedetection` | `BASE_URL` | `${BASE_URL}` |
| `changedetection` | `PLAYWRIGHT_DRIVER_URL` | `${PLAYWRIGHT_DRIVER_URL}` |
| `changedetection` | `FETCH_WORKERS` | `5` |
| `changedetection` | `MINIMUM_SECONDS_RECHECK_TIME` | `60` |
| `changedetection` | `HIDE_REFERER` | `true` |
| `browser-sockpuppet-chrome` | `SCREEN_WIDTH` | `1920` |
| `browser-sockpuppet-chrome` | `SCREEN_HEIGHT` | `1080` |
| `browser-sockpuppet-chrome` | `SCREEN_DEPTH` | `16` |
| `browser-sockpuppet-chrome` | `MAX_CONCURRENT_CHROME_PROCESSES` | `10` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `changedetection-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Custom networks:** This stack defines custom networks (`changedetection_net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:24 UTC_

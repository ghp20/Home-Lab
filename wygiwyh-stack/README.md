# Wygiwyh Stack

> WYGIWYH (What You Get Is What You Have) is a custom inventory and asset management tool.

**Status:** 🔴 Inactive  
**Stack:** `wygiwyh-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `db` | `postgres:${PG_TAG:-15}` | — | 🔴 — |
| `wygiwyh` | `eitchtee/wygiwyh:${KK_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `db` | `no-new-privileges:true` | Config/data |
| `db` | `${STORAGE_ROOT}/db:/var/lib/postgresql/data:rw` | Config/data |
| `wygiwyh` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `wygiwyh` | `${STORAGE_ROOT}/media:/app/media:rw` | Config/data |
| `wygiwyh` | `${STORAGE_ROOT}/static:/app/static:rw` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `wygiwyh-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `on-failure:5` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:23 UTC_

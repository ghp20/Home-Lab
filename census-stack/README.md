# Census Stack

> Census provides infrastructure asset inventory and configuration management tracking.

**Status:** 🔴 Inactive  
**Stack:** `census-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `census-server` | `ghcr.io/selfhosters-cc/container-census:${CENSUS_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `census-server` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `census-server` | `${DOCKER_SOCK}:/var/run/docker.sock` | Config/data |
| `census-server` | `${STORAGE_ROOT}/server:/app/data` | Config/data |
| `census-server` | `${STORAGE_ROOT}/config:/app/config` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `census-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-07-13 17:00 UTC_

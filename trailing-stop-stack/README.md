# Trailing Stop Stack

> Trailing Stop provides automated trailing stop-loss order management for cryptocurrency and stock trading.

**Status:** 🔴 Inactive  
**Stack:** `trailing-stop-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `db` | `postgres:16` | — | 🔴 — |
| `agent` | `trailing_stop_agent:latest` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `db` | `192.168.1.12:5433:5432` | Config/data |
| `db` | `/path/to/data/apps_config/ron/trailing-stop/postgres:/var/lib/postgresql/data` | Config/data |
| `agent` | `/path/to/data/apps_config/ron/trailing-stop/logs:/app/logs` | Config/data |
| `agent` | `/path/to/data/apps_config/ron/trailing-stop/backtest:/app/backtest_results` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `trailing-stop-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`trading_net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-07-13 17:00 UTC_

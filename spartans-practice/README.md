# Spartans Practice

> The Spartans Practice stack.

**Status:** 🟢 Active  
**Stack:** `spartans-practice`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `postgres:15` | — | 🔴 — |
| `backend` | `spartans-practice-backend:latest` | — | 🔴 — |
| `frontend` | `spartans-practice-frontend:latest` | 3001:8080 | 🔴 — |

## 🔌 Port Reference

| Service | Mapping | Protocol |
|---------|---------|----------|
| `frontend` | `3001:8080` | TCP |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `/path/to/data/apps_config/spartans-practice/postgres-data:/var/lib/postgresql/data` | Config/data |
| `postgres` | `/path/to/data/apps_config/spartans-practice/backups:/backups` | Config/data |
| `backend` | `/path/to/data/apps_config/spartans-practice/uploads:/app/assets/uploads` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `spartans-practice`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`backend-net, spartans-platform-net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-08-17 17:00 UTC_

# Crowdsec Stack

> CrowdSec is a collaborative intrusion prevention and detection system that shares threat intelligence across its community.

**Status:** 🟢 Active  
**Stack:** `crowdsec-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `crowdsec` | `crowdsecurity/crowdsec:${CS_TAG:-latest}` | — | 🔴 — |
| `crowdsec-cloudflare-bouncer` | `crowdsecurity/cloudflare-bouncer:${CF_BOUNCER_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `crowdsec` | `${STORAGE_ROOT}/config/acquis.yaml:/etc/crowdsec/acquis.yaml` | Config/data |
| `crowdsec` | `${STORAGE_ROOT}/data:/var/lib/crowdsec/data` | Config/data |
| `crowdsec` | `/var/log:/var/log:ro` | Config/data |
| `crowdsec` | `${NPM_LOGS_ROOT}:/var/log/nginx:ro` | Config/data |
| `crowdsec` | `${METRICS_PORT}:6060` | Config/data |
| `crowdsec` | `${SYSLOG_PORT}:1514/udp` | Config/data |
| `crowdsec-cloudflare-bouncer` | `${STORAGE_ROOT}/cloudflare-bouncer/cfg.yaml:/etc/crowdsec/bouncers/crowdsec-cloudflare-bouncer.yaml:ro` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `crowdsec` | `TZ` | `${TZ}` |
| `crowdsec` | `COLLECTIONS` | `${COLLECTIONS}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `crowdsec-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Custom networks:** This stack defines custom networks (`crowdsec-net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-06-22 17:01 UTC_

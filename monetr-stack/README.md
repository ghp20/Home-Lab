# Monetr Stack

> Monetr is a budgeting and financial planning application designed for long-term financial management.

**Status:** 🔴 Inactive  
**Stack:** `monetr-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `monetr` | `ghcr.io/monetr/monetr:${MONETR_TAG:-latest}` | — | 🔴 — |
| `monetr-postgres` | `postgres:${PG_TAG:-16-alpine}` | — | 🔴 — |
| `monetr-valkey` | `valkey/valkey:${VALKEY_TAG:-8-alpine}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `monetr` | `${HOST_PORT}:4000` | Config/data |
| `monetr` | `${STORAGE_ROOT}/config:/etc/monetr` | Config/data |
| `monetr-postgres` | `${STORAGE_ROOT}/postgres:/var/lib/postgresql/data` | Config/data |
| `monetr-valkey` | `${STORAGE_ROOT}/valkey:/data` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `monetr-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `monetr-postgres`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:24 UTC_

# Firefly3 Stack

> Firefly III is a self-hosted personal finance manager for budgeting, expense tracking, and financial reporting.

**Status:** 🔴 Inactive  
**Stack:** `firefly3-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `firefly-db` | `mariadb:${DB_TAG:-10.6}` | — | 🔴 — |
| `firefly-iii` | `fireflyiii/core:${FF_TAG:-latest}` | — | 🔴 — |
| `firefly-importer` | `fireflyiii/data-importer:${IMP_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `firefly-db` | `${STORAGE_ROOT}/database:/var/lib/mysql` | Config/data |
| `firefly-iii` | `${CORE_HOST_PORT}:${CORE_CONTAINER_PORT}` | Config/data |
| `firefly-iii` | `${STORAGE_ROOT}/uploads:/var/www/html/storage/upload` | Config/data |
| `firefly-importer` | `${IMP_HOST_PORT}:${IMP_CONTAINER_PORT}` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `firefly3-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `firefly-db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `always, unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:23 UTC_

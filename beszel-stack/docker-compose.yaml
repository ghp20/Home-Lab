version: "3.8"

services:
  beszel:
    image: henrygd/beszel:latest
    container_name: beszel
    restart: unless-stopped
    ports:
      - "8097:8090"   # Change 8095 if needed
    volumes:
      - /mnt/storagepool/apps_config/beszel/hub-data:/beszel_data
      - /mnt/storagepool/apps_config/beszel/socket:/beszel_socket
    environment:
      - TZ=${TZ}

  beszel-agent:
    image: henrygd/beszel-agent:latest
    container_name: beszel-agent
    restart: unless-stopped
    network_mode: host
    privileged: true
    volumes:
      - /mnt/storagepool/apps_config/beszel/agent-data:/var/lib/beszel-agent
      - /mnt/storagepool/apps_config/beszel/socket:/beszel_socket
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - LISTEN=/beszel_socket/beszel.sock
      - HUB_URL=${HUB_URL}
      - TOKEN=${TOKEN}
      - KEY=${KEY}
      - TZ=${TZ}
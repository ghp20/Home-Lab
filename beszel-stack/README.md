# Beszel Stack

> Beszel is a lightweight server monitoring tool with a clean web UI for tracking system resources across multiple servers.

**Status:** 🟢 Active  
**Stack:** `beszel-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `beszel` | `henrygd/beszel:${BESZEL_TAG:-latest}` | — | 🔴 — |
| `beszel-agent` | `henrygd/beszel-agent:${AGENT_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `beszel` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `beszel` | `${STORAGE_ROOT}/hub-data:/beszel_data` | Config/data |
| `beszel` | `${STORAGE_ROOT}/socket:/beszel_socket` | Config/data |
| `beszel-agent` | `${STORAGE_ROOT}/agent-data:/var/lib/beszel-agent` | Config/data |
| `beszel-agent` | `${STORAGE_ROOT}/socket:/beszel_socket` | Config/data |
| `beszel-agent` | `/var/run/docker.sock:/var/run/docker.sock:ro` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `beszel` | `TZ` | `${TZ}` |
| `beszel-agent` | `LISTEN` | `${LISTEN}` |
| `beszel-agent` | `HUB_URL` | `${HUB_URL}` |
| `beszel-agent` | `TOKEN` | `changeme` |
| `beszel-agent` | `KEY` | `changeme` |
| `beszel-agent` | `TZ` | `${TZ}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `beszel-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Docker socket mounted:** `beszel-agent` has access to the Docker socket — ensure this is intentional for security.

---
_Auto-generated by Alex • Last updated: 2026-06-22 17:01 UTC_

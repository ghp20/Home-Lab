# Music Assistant Stack

> Music Assistant is a music library manager and player that integrates with streaming services and your local music collection for Home Assistant.

**Status:** 🟢 Active  
**Stack:** `music-assistant-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `music-assistant-server` | `ghcr.io/music-assistant/server:${MA_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `music-assistant-server` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `music-assistant-server` | `${STORAGE_ROOT}:/data:rw` | Config/data |
| `music-assistant-server` | `${MUSIC_ROOT}:/media:ro` | Config/data |
| `music-assistant-server` | `apparmor:unconfined` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `music-assistant-server` | `LOG_LEVEL` | `${LOG_LEVEL}` |
| `music-assistant-server` | `MASS_DISABLE_ZEROCONF` | `true` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `music-assistant-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:24 UTC_

# Spartans Maintenance

> The Spartans Maintenance stack.

**Status:** 🟢 Active  
**Stack:** `spartans-maintenance`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `postgres:15` | — | 🔴 — |
| `backend` | `spartans-maintenance-backend:latest` | — | 🔴 — |
| `frontend` | `spartans-maintenance-frontend:latest` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `/path/to/data/apps_config/spartans-maintenance/postgres-data:/var/lib/postgresql/data` | Config/data |
| `backend` | `/path/to/data/apps_config/spartans-maintenance/db:/data` | Config/data |
| `frontend` | `${APP_PORT}:80` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `spartans-maintenance`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`backend-net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-06-08 17:00 UTC_

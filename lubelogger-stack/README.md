# Lubelogger Stack

> LubeLogger is a web-based vehicle maintenance and service record tracker for managing oil changes, repairs, and inspections.

**Status:** 🟢 Active  
**Stack:** `lubelogger-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `lubelogger` | `ghcr.io/hargata/lubelogger:${LL_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `lubelogger` | `${STORAGE_ROOT}/data:/App/data` | Config/data |
| `lubelogger` | `${STORAGE_ROOT}/keys:/App/keys` | Config/data |
| `lubelogger` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `lubelogger-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-24 17:00 UTC_

# Dispatcharr Stack

> DispatchArr is a dispatch and scheduling management tool.

**Status:** 🔴 Inactive  
**Stack:** `dispatcharr-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `dispatcharr` | `ghcr.io/dispatcharr/dispatcharr:${DISPATCHARR_TAG:-latest}` | — | 🔴 — |
| `gluetun-iptv` | `qmcgaw/gluetun:${GLUETUN_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `dispatcharr` | `${STORAGE_ROOT_DISPATCHARR}:/data` | Config/data |
| `dispatcharr` | `/dev/dri/renderD128:/dev/dri/renderD128` | Config/data |
| `dispatcharr` | `/dev/dri/card0:/dev/dri/card0` | Config/data |
| `gluetun-iptv` | `/dev/net/tun:/dev/net/tun` | Config/data |
| `gluetun-iptv` | `${HOST_PORT}:9191` | Config/data |
| `gluetun-iptv` | `${STORAGE_ROOT_GLUETUN}:/gluetun-iptv` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `dispatcharr` | `DJANGO_SECRET_KEY` | `changeme` |
| `dispatcharr` | `DISPATCHARR_ENV` | `${DISPATCHARR_ENV}` |
| `dispatcharr` | `REDIS_HOST` | `${REDIS_HOST}` |
| `dispatcharr` | `CELERY_BROKER_URL` | `${CELERY_BROKER_URL}` |
| `dispatcharr` | `DISPATCHARR_LOG_LEVEL` | `${DISPATCHARR_LOG_LEVEL}` |
| `gluetun-iptv` | `VPN_SERVICE_PROVIDER` | `${VPN_SERVICE_PROVIDER}` |
| `gluetun-iptv` | `VPN_TYPE` | `${VPN_TYPE}` |
| `gluetun-iptv` | `OPENVPN_USER` | `${OPENVPN_USER}` |
| `gluetun-iptv` | `OPENVPN_PASSWORD` | `changeme` |
| `gluetun-iptv` | `SERVER_COUNTRIES` | `${SERVER_COUNTRIES}` |
| `gluetun-iptv` | `SERVER_CITIES` | `${SERVER_CITIES}` |
| `gluetun-iptv` | `SERVER_SELECTION` | `${SERVER_SELECTION}` |
| `gluetun-iptv` | `TZ` | `${TZ}` |
| `gluetun-iptv` | `DNS_ADDRESS` | `${DNS_ADDRESS}` |
| `gluetun-iptv` | `DOT` | `${DOT}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `dispatcharr-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Device mapping:** `dispatcharr` maps a device (`/dev/dri/renderD128:/dev/dri/renderD128`). Ensure the device exists on the host.
3. **Device mapping:** `gluetun-iptv` maps a device (`/dev/net/tun:/dev/net/tun`). Ensure the device exists on the host.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:24 UTC_

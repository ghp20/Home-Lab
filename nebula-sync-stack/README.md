# Nebula Sync Stack

> Nebula Sync provides configuration synchronization across multiple Pi-hole instances for consistent DNS blocking.

**Status:** 🟢 Active  
**Stack:** `nebula-sync-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `orbital-sync` | `orbital-sync:local-v6` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `orbital-sync` | `/path/to/data/apps_config/orbital-sync/data:/app/data` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `orbital-sync` | `PIHOLE_VERSION` | `${PIHOLE_VERSION}` |
| `orbital-sync` | `PRIMARY_HOST_BASE_URL` | `${PRIMARY_HOST_BASE_URL}` |
| `orbital-sync` | `PRIMARY_HOST_PASSWORD` | `changeme` |
| `orbital-sync` | `PRIMARY_HOST_PATH` | `${PRIMARY_HOST_PATH}` |
| `orbital-sync` | `SECONDARY_HOSTS_1_BASE_URL` | `${SECONDARY_HOSTS_1_BASE_URL}` |
| `orbital-sync` | `SECONDARY_HOSTS_1_PASSWORD` | `changeme` |
| `orbital-sync` | `SECONDARY_HOSTS_1_PATH` | `${SECONDARY_HOSTS_1_PATH}` |
| `orbital-sync` | `INTERVAL_MINUTES` | `${INTERVAL_MINUTES}` |
| `orbital-sync` | `VERBOSE` | `true` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `nebula-sync-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-06-29 17:00 UTC_

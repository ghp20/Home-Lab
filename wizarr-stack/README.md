# Wizarr Stack

> Wizarr is a tool for managing invitations and onboarding for Plex/Jellyfin media server users.

**Status:** 🟢 Active  
**Stack:** `wizarr-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `wizarr` | `ghcr.io/wizarrrr/wizarr:${WIZARR_TAG:-latest}` | — | 🔴 — |
| `wizarr_postgres` | `postgres:${PG_TAG:-16-alpine}` | — | 🔴 — |
| `wizarr_redis` | `redis:${REDIS_TAG:-7-alpine}` | — | 🔴 — |
| `wizarr_memcached` | `memcached:${MEMCACHED_TAG:-1.6-alpine}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `wizarr` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `wizarr` | `${STORAGE_ROOT}/database:/data/database` | Config/data |
| `wizarr` | `${STORAGE_ROOT}/storage:/data/storage` | Config/data |
| `wizarr_postgres` | `${STORAGE_ROOT}/database/pgdata:/var/lib/postgresql/data` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `wizarr-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `wizarr_postgres, wizarr_redis`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-03 17:00 UTC_

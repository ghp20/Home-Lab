# Monitoring Stack

> Full monitoring stack with Prometheus, Grafana, Alertmanager, and exporters for comprehensive infrastructure and application observability.

**Status:** 🟢 Active  
**Stack:** `monitoring-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `prometheus` | `prom/prometheus:${PROM_TAG:-latest}` | — | 🔴 — |
| `grafana` | `grafana/grafana:${GRAFANA_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `prometheus` | `${STORAGE_ROOT}/prometheus/config/prometheus.yml:/etc/prometheus/prometheus.yml:ro` | Config/data |
| `prometheus` | `${STORAGE_ROOT}/prometheus/data:/prometheus` | Config/data |
| `prometheus` | `${PROM_PORT}:${PROM_PORT}` | Config/data |
| `grafana` | `${STORAGE_ROOT}/grafana/data:/var/lib/grafana` | Config/data |
| `grafana` | `${GRAFANA_HOST_PORT}:${GRAFANA_CONTAINER_PORT}` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `grafana` | `TZ` | `${TZ}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `monitoring-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Custom networks:** This stack defines custom networks (`monitoring-net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-06-08 17:00 UTC_

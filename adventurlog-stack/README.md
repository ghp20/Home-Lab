# Adventurlog Stack

> AdventurLog is an adventure and travel logging application for recording trips and outdoor experiences.

**Status:** 🔴 Inactive  
**Stack:** `adventurlog-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `db` | `postgis/postgis:${PG_TAG:-16-3.5}` | — | 🔴 — |
| `backend` | `ghcr.io/seanmorley15/adventurelog-backend:${BACKEND_TAG:-latest}` | — | 🔴 — |
| `frontend` | `ghcr.io/seanmorley15/adventurelog-frontend:${FRONTEND_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `db` | `${STORAGE_ROOT}/postgres:/var/lib/postgresql/data` | Config/data |
| `backend` | `${BACKEND_HOST_PORT}:${BACKEND_CONTAINER_PORT}` | Config/data |
| `backend` | `${STORAGE_ROOT}/media:/code/media` | Config/data |
| `frontend` | `${FRONTEND_HOST_PORT}:${FRONTEND_CONTAINER_PORT}` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `adventurlog-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-05-05 19:23 UTC_

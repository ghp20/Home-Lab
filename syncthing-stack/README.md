# Syncthing Stack

> Syncthing is a continuous file synchronization program that syncs files between two or more devices with end-to-end encryption.

**Status:** 🟢 Active  
**Stack:** `syncthing-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `syncthing` | `syncthing/syncthing:${ST_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `syncthing` | `${PATH_MOVIES}:/data/movies` | Config/data |
| `syncthing` | `${PATH_TV}:/data/tv` | Config/data |
| `syncthing` | `${STORAGE_ROOT}:/var/syncthing` | Config/data |
| `syncthing` | `${UI_PORT}:${UI_PORT}` | Config/data |
| `syncthing` | `${SYNC_PORT}:${SYNC_PORT}/tcp` | Config/data |
| `syncthing` | `${SYNC_PORT}:${SYNC_PORT}/udp` | Config/data |
| `syncthing` | `${DISCOVERY_PORT}:${DISCOVERY_PORT}/udp` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `syncthing-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-06-01 17:00 UTC_

services:
  app:
    image: ghcr.io/javedh-dev/tracktor:${TR_TAG:-latest}
    container_name: tracktor-app
    restart: unless-stopped
    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"
    volumes:
      - ${STORAGE_ROOT}:/data
    environment:
      - TRACKTOR_DEMO_MODE=${DEMO_MODE}
      - FORCE_DATA_SEED=${SEED_DATA}
      - CORS_ORIGINS=${CORS_ORIGINS}
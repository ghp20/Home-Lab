# Mealie Stack

> Mealie is a self-hosted recipe manager and meal planner with a clean web interface, supporting recipe imports from URLs.

**Status:** 🟢 Active  
**Stack:** `mealie-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `postgres:${PG_TAG:-17}` | — | 🔴 — |
| `mealie` | `ghcr.io/mealie-recipes/mealie:${MEALIE_TAG:-v3.9.2}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `${STORAGE_ROOT}/postgres:/var/lib/postgresql/data` | Config/data |
| `mealie` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `mealie` | `${STORAGE_ROOT}/mealie/data:/app/data` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `mealie-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `always` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`default`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-07-27 17:01 UTC_

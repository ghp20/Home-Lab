# Homarr Stack

> Homarr is a sleek, customizable dashboard for managing and accessing all your homelab services from a single landing page.

**Status:** 🟢 Active  
**Stack:** `homarr-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `homarr` | `ghcr.io/homarr-labs/homarr:${HOMARR_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `homarr` | `${APPDATA_DIR}:/appdata` | Config/data |
| `homarr` | `${CONFIGS_DIR}:/configs` | Config/data |
| `homarr` | `${ICONS_DIR}:/icons` | Config/data |
| `homarr` | `/var/run/docker.sock:/var/run/docker.sock:ro` | Config/data |
| `homarr` | `${HOST_PORT:-30100}:7575` | Config/data |
| `homarr` | `PUID=${PUID:-1000}` | Config/data |
| `homarr` | `PGID=${PGID:-1000}` | Config/data |
| `homarr` | `TZ=${TZ:-America/Los_Angeles}` | Config/data |
| `homarr` | `UMASK=${UMASK:-002}` | Config/data |
| `homarr` | `AUTH_PROVIDERS=${AUTH_PROVIDERS:-credentials}` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `homarr` | `SECRET_ENCRYPTION_KEY` | `changeme` |
| `homarr` | `DB_DIALECT` | `sqlite` |
| `homarr` | `DB_URL` | `/appdata/db/db.sqlite` |
| `homarr` | `DB_DRIVER` | `better-sqlite3` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `homarr-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
2. **Docker socket mounted:** `homarr` has access to the Docker socket — ensure this is intentional for security.
3. **Custom networks:** This stack defines custom networks (`homarr-network`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-06-29 17:01 UTC_

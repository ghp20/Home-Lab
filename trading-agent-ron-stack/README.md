# Trading Agent Ron Stack

> Trading Agent Ron is an automated trading bot infrastructure for monitoring and executing trading strategies.

**Status:** 🔴 Inactive  
**Stack:** `trading-agent-ron-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `trading-agent-ron` | `trading-agent-ron:${RON_TAG:-latest}` | — | 🔴 — |
| `trading-postgres-ron` | `postgres:${PG_TAG:-15-alpine}` | — | 🔴 — |
| `trading-redis-ron` | `redis:${REDIS_TAG:-7-alpine}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `trading-agent-ron` | `${STORAGE_ROOT}/config/config.yaml:/app/config/config.yaml:ro` | Config/data |
| `trading-agent-ron` | `${STORAGE_ROOT}/config/.env:/app/config/.env:ro` | Config/data |
| `trading-agent-ron` | `${STORAGE_ROOT}/models:/app/models` | Config/data |
| `trading-agent-ron` | `${STORAGE_ROOT}/logs:/app/logs` | Config/data |
| `trading-agent-ron` | `${STORAGE_ROOT}/data:/app/data` | Config/data |
| `trading-agent-ron` | `${STORAGE_ROOT}/src/:/app/src/` | Config/data |
| `trading-postgres-ron` | `${STORAGE_ROOT}/postgres:/var/lib/postgresql/data` | Config/data |
| `trading-redis-ron` | `${STORAGE_ROOT}/redis:/data` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `trading-agent-ron-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `trading-postgres-ron, trading-redis-ron`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-06-08 17:00 UTC_

# Wireguard Us Server Stack

> WireGuard VPN server configured for US exit, providing secure tunneling with a US-based IP address.

**Status:** 🟢 Active  
**Stack:** `wireguard-us-server-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `wireguard-us-server` | `lscr.io/linuxserver/wireguard:${WG_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `wireguard-us-server` | `${STORAGE_ROOT}:/config` | Config/data |
| `wireguard-us-server` | `${MODULES_PATH}:/lib/modules:ro` | Config/data |
| `wireguard-us-server` | `${SSH_PORT}:${SSH_PORT}/tcp` | Config/data |
| `wireguard-us-server` | `${HTTPS_PORT}:${HTTPS_PORT}/tcp` | Config/data |

## 🔧 Environment Variables

| Service | Variable | Value |
|---------|----------|-------|
| `wireguard-us-server` | `PUID` | `${PUID}` |
| `wireguard-us-server` | `PGID` | `${PGID}` |
| `wireguard-us-server` | `TZ` | `${TZ}` |
| `wireguard-us-server` | `SERVERMODE` | `${SERVER_MODE}` |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `wireguard-us-server-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-06-15 17:00 UTC_

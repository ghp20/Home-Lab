# Seerr Stack

> Overseerr / Jellyseerr — a request management portal for your media server, allowing users to request movies and TV shows.

**Status:** 🟢 Active  
**Stack:** `seerr-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `seerr-db` | `postgres:${POSTGRES_TAG:-16}` | — | 🔴 — |
| `seerr` | `ghcr.io/seerr-team/seerr:${SEERR_TAG:-latest}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `seerr-db` | `${STORAGE_ROOT}/db:/var/lib/postgresql/data` | Config/data |
| `seerr` | `${HOST_PORT}:5055` | Config/data |
| `seerr` | `${STORAGE_ROOT}/config:/app/config` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `seerr-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `seerr-db`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-10 17:00 UTC_

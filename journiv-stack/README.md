# Journiv Stack

> Journiv is a journaling and life-tracking application for personal reflection and habit tracking.

**Status:** 🟢 Active  
**Stack:** `journiv-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `postgres` | `postgres:15` | — | 🔴 — |
| `redis` | `redis:7` | — | 🔴 — |
| `celery-worker` | `swalabtech/journiv-app:latest` | — | 🔴 — |
| `app` | `swalabtech/journiv-app:latest` | 8000:8000 | 🔴 — |

## 🔌 Port Reference

| Service | Mapping | Protocol |
|---------|---------|----------|
| `app` | `8000:8000` | TCP |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `postgres` | `/var/run/postgresql:rw,noexec,nosuid,size=64m` | Config/data |
| `postgres` | `/path/to/data/apps_config/journiv/postgres_data:/var/lib/postgresql/data` | Config/data |
| `redis` | `/path/to/data/apps_config/journiv/redis_data:/data` | Config/data |
| `celery-worker` | `/path/to/data/apps_config/journiv/app_data:/data` | Config/data |
| `app` | `/path/to/data/apps_config/journiv/app_data:/data` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `journiv-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `postgres, redis`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`backend, frontend`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-08-03 17:00 UTC_

# Karakeep Stack

> Karakeep is a bookmark and knowledge management system for saving and organizing web content.

**Status:** 🟢 Active  
**Stack:** `karakeep-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `web` | `ghcr.io/karakeep-app/karakeep:${KK_TAG:-release}` | — | 🔴 — |
| `chrome` | `gcr.io/zenika-hub/alpine-chrome:${CHROME_TAG:-124}` | — | 🔴 — |
| `meilisearch` | `getmeili/meilisearch:${MEILI_TAG:-v1.13.3}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `web` | `${HOST_PORT}:${CONTAINER_PORT}` | Config/data |
| `web` | `${STORAGE_ROOT}/data:/data` | Config/data |
| `meilisearch` | `${STORAGE_ROOT}/meilisearch:/meili_data` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `karakeep-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.

---
_Auto-generated by Alex • Last updated: 2026-08-31 17:00 UTC_

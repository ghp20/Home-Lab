# Graylog Stack

> Graylog is a centralized log management platform that collects, indexes, and analyzes log data from all your applications and infrastructure.

**Status:** 🔴 Inactive  
**Stack:** `graylog-stack`

## 📦 Services

| Container | Image | Port Mappings | Status |
|-----------|-------|---------------|--------|
| `mongodb` | `mongo:${MONGO_TAG:-7}` | — | 🔴 — |
| `datanode` | `graylog/graylog-datanode:${DATANODE_TAG:-7.0.4}` | — | 🔴 — |
| `graylog` | `graylog/graylog:${GRAYLOG_TAG:-7.0.4}` | — | 🔴 — |

## 💾 Volumes

| Service | Mount | Notes |
|---------|-------|-------|
| `mongodb` | `${STORAGE_ROOT}/mongodb-data:/data/db` | Config/data |
| `datanode` | `${STORAGE_ROOT}/datanode-data:/var/lib/graylog-datanode` | Config/data |
| `datanode` | `${DATANODE_PORT}:${DATANODE_PORT}` | Config/data |
| `graylog` | `${STORAGE_ROOT}/graylog-data:/usr/share/graylog/data` | Config/data |
| `graylog` | `${WEB_PORT}:${WEB_PORT}/tcp` | Config/data |
| `graylog` | `${GELF_PORT}:${GELF_PORT}/udp` | Config/data |
| `graylog` | `${GELF_PORT}:${GELF_PORT}/tcp` | Config/data |
| `graylog` | `${SYSLOG_PORT}:${SYSLOG_PORT}/udp` | Config/data |
| `graylog` | `${SYSLOG_PORT}:${SYSLOG_PORT}/tcp` | Config/data |

## 📊 Resource Recommendations

_No explicit resource limits configured. Defaults apply._

## 🚀 Deployment via Portainer

1. Navigate to **Portainer** → **Stacks** → **Add stack**
2. Name the stack: `graylog-stack`
3. Paste the `docker-compose.yaml` content into the web editor
4. Configure required environment variables (see above)
5. Set up required volume mount paths on the host
6. Click **Deploy the stack**

## ⚠️ Things to Know Before Deploying

1. **Database dependency:** This stack includes `mongodb`. Ensure the database container is healthy before starting dependent services.
2. **Restart policy:** `unless-stopped` — containers will restart automatically unless explicitly stopped.
3. **Custom networks:** This stack defines custom networks (`graylog_net`). Other stacks sharing these networks can communicate directly.

---
_Auto-generated by Alex • Last updated: 2026-08-10 17:00 UTC_
